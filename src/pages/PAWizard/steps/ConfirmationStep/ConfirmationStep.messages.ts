import { defineMessages } from "react-intl";

export default defineMessages({
  heading: { id: "confirmation.heading", defaultMessage: "Policy Bound Successfully!" },
  description: { id: "confirmation.description", defaultMessage: "Your Personal Auto policy has been issued." },
});
