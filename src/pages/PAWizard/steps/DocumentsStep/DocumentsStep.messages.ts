import { defineMessages } from "react-intl";

export default defineMessages({
  heading: { id: "documentsStep.heading", defaultMessage: "Policy Documents" },
  description: { id: "documentsStep.description", defaultMessage: "Upload any required documents (ID, prior policy, etc.)." },
});
