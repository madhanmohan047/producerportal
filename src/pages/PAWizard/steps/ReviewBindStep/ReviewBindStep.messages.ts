import { defineMessages } from "react-intl";

export default defineMessages({
  heading: { id: "reviewBind.heading", defaultMessage: "Review & Bind Policy" },
  description: { id: "reviewBind.description", defaultMessage: "Please review all information before binding." },
});
