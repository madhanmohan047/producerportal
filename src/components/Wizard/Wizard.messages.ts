import { defineMessages } from "react-intl";

export default defineMessages({
  startNewSubmission: {
    id: "wizard.startNewSubmission",
    defaultMessage: "Start new submission",
  },
  draftAutoSaved: {
    id: "wizard.draftAutoSaved",
    defaultMessage: "Draft auto-saved",
  },
});
